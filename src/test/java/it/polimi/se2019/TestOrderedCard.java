package it.polimi.se2019;

import it.polimi.se2019.model.AmmoList;
import it.polimi.se2019.model.OrderedCardList;
import org.junit.Test;
import org.junit.jupiter.api.Order;
import it.polimi.se2019.model.AmmoCard;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class TestOrderedCard{
/*FOR MAVEN PURPOSE
    OrderedCardList orderedCardList=new OrderedCardList("");
    OrderedCardList orderedCardList2=new OrderedCardList("");

    AmmoList ammoList=new AmmoList();
    AmmoCard card= new AmmoCard("1",ammoList, false);
    AmmoCard card1= new AmmoCard("2",ammoList, false);

    @Test
    public void test(){

        orderedCardList.addCard(card);
        orderedCardList.addCard(card1);
        orderedCardList.getCards();
        orderedCardList.getCard("1");
        orderedCardList.shuffle();
        orderedCardList.moveCardTo(orderedCardList2, "1");

        assertEquals(card, orderedCardList2.getCard("1"));
        assertEquals(card, orderedCardList2.getFirstCard());
        assertEquals(false, orderedCardList2.removeCard("2"));

       orderedCardList.moveAllCardsTo(orderedCardList2);
       assertEquals(card1, orderedCardList2.getCard("2"));
       assertEquals(false, orderedCardList2.moveCardTo(orderedCardList,"3"));
       assertEquals(null, orderedCardList.getCard("e"));


    }
    */
}
