package it.polimi.se2019.ModelTest;

import it.polimi.se2019.model.Kill;
import it.polimi.se2019.model.Player;
import org.junit.Test;

import static junit.framework.TestCase.fail;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class TestKill{
    /*FOR MAVEN PURPOSE
    Kill kill=new Kill();
    Player p= new Player();
    Player p1=new Player();

    @Test
    public void testWhatsLeft()throws Exception{

        assertEquals(false, kill.isOverKill());
        assertEquals(true, kill.isSkull());

        kill.setKillingPlayer(p);
        kill.setOverkillingPlayer(p);
        assertEquals(p, kill.getKillingPlayer());
        assertEquals(p, kill.getOverKillingPlayer());

        assertEquals(false, kill.isSkull());
        assertEquals(true, kill.isOverKill());

        p1=kill.getKillingPlayer();
        assertEquals(p,p1);


        try{
            kill.setKillingPlayer(p);
            fail();
        }catch(Exception e){};

        try{
            kill=new Kill();
            kill.setOverkillingPlayer(p);
            fail();
        }catch(Exception e){};

        try{
            kill=new Kill();
            p1=kill.getKillingPlayer();
            fail();

        }catch(Exception e){};

        try{

            kill=new Kill();
            p1=kill.getOverKillingPlayer();
            fail();


        }catch(Exception e){};



    }
    */
}
