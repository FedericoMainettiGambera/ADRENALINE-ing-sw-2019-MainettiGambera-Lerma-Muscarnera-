package it.polimi.se2019.ModelTest;

import it.polimi.se2019.model.AmmoCubes;
import it.polimi.se2019.model.enumerations.AmmoCubesColor;
import org.junit.Test;

import static org.junit.jupiter.api.Assertions.*;

public class TestAmmoCube{
    /*FOR MAVEN PURPOSE

    AmmoCubes ammo=new AmmoCubes(AmmoCubesColor.yellow);

    @Test
    public void testGetters(){
        assertEquals(AmmoCubesColor.yellow, ammo.getColor());
        assertEquals(0,ammo.getQuantity());

        ammo.addQuantity(3);
        assertEquals(3,ammo.getQuantity());
        ammo.addQuantity(5);
        assertEquals(3,ammo.getQuantity());
    }
    @Test
    public void testOther(){
        ammo.addQuantity(3);
        ammo.subQuantity(2);
        assertEquals(1, ammo.getQuantity());
        ammo.subQuantity(2);
        assertEquals(1,ammo.getQuantity());
        assertEquals(false, ammo.canSubQuantity(2));
        assertEquals(false, ammo.subQuantity(2));
        assertNotEquals(true, ammo.subQuantity(2));
        assertEquals(true, ammo.canSubQuantity(1));
        assertEquals(true, ammo.subQuantity(1));
        assertEquals(0, ammo.getQuantity());
    }
    */
}
